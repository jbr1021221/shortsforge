# Engineering Rules

* Use MVVM architecture
* Use Hilt dependency injection
* Use Room for persistent systems
* Avoid SharedPreferences for new features
* Keep workers single responsibility
* Heavy tasks must run foreground
* Never block Main thread
* Use suspend functions for IO
* Reuse repositories when possible
* Preserve backward compatibility
* Avoid large refactors in one step
